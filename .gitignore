# 生成物/临时
*.log
.DS_Store
Thumbs.db

# 本仓库是**产物仓库**：由上游的 _build_public_docs.cjs 生成，原子替换整个目录。
# 所以在这里直接改文件会在下次生成时丢失 —— 要改请改上游的源。

# ★ 拦截台账（2026-09-26 加）：_check_all.cjs 每次"有项报红"时追加一行
#   它是**本机运行历史**（跟机器、跟时间绑定），不该进版本库。
#   要看它的摘要：跑 `node checks/_selftest.cjs`（末尾会打印"拦到过什么 / 从没拦到过什么"）。
_拦截台账.jsonl
