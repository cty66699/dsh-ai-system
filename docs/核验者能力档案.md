<!-- 本文件由**脱敏生成器**从内部真文档自动生成（已脱敏）。请勿手工编辑 —— 手工副本必漂移。 -->
# 各模型能力证据档案（对称标准）

> 解决 **F-18**：*「DeepSeek 的能力档位全文缺席 —— 唯一没给排名的模型拿走了全部高杠杆角色（规格、判定、合并）；Kimi 的排名同样缺席。**证据要求只对 GPT-6 严格，这是选择性质疑。**」*
>
> **本文件的立场：不对任何模型做无证据的断言，也不给任何一个模型免证据的特权。**
> 凡是本工作区**真实测过的**，写进来；**没测过的，明确写"空白"**，不拿榜单填。

---

## 〇、证据分级（本表只用前两级）

| 级别 | 含义 |
|---|---|
| **实测·自有** | 本工作区跑过、有原始产物与数字，**可直接引用** |
| **实测·单次** | 跑过但 **n=1**，只能作线索，不能作结论 |
| **未测** | 明确写「空白」 |
| ~~榜单~~ | **本表不采用**（安全边界③：外部榜单只用于筛候选，不作选型依据） |

---

## 一、主控（DeepSeek）

**角色**：规格员 · 判定 · 裁定 · 合并 · 长任务主力（**全部高杠杆位置**）

| 证据 | 内容 | 级别 |
|---|---|---|
| **L-001 长任务对比** | 文档补丁任务（真实待办）：DeepSeek 找到 **6 条额外矛盾**，Claude Opus 5 与 GPT-6 各 **0 条**；成本 **¥（略）** vs ¥（略）–0.43 vs ¥（略）；缓存命中率 **88.1%** vs 39.5% | **实测·单次** |
| **W-001 配对检验** | 亚像素裂缝宽度任务：DeepSeek 与 GLM-5.3 **不可区分**（中位差 −0.00003 px，95%CI [−0.00033, +0.00031] 跨 0，符号检验 **p=0.9437**） | **实测·自有** |
| **缓存/成本曲线** | 混合 ≈**¥（略）/M**；缓存命中 ¥（略）/M；长任务 **74% 成本 = 每轮重发的累积上下文** | **实测·自有** |

### ⚠️ 对本角色的选择性质疑（必须自己指出）
**它是唯一没有能力排名的模型，却拿走了规格 / 判定 / 裁定 / 合并全部高杠杆位置。**
上述证据能支持的是：**「在长文档类任务上，它不劣于、且更便宜」** —— **不能**支持「它足够胜任规格员与裁定者」。**那是本体系最大的未证假设。**

---

## 二、Kimi K3（核验者）

**角色**：T1 事实核查 · T2 推导复核 · T4 一致性对账 · **T6 裁定复核**

| 证据 | 内容 | 级别 |
|---|---|---|
| **T6 首次运行** | 复核主控对 21 条漏洞的裁定 —— **判「裁定需返工」并给出具体证据**：查出 1 条不实（F-01）、2 条过宽（F-02/F-14）、1 条半裁（F-16②）、**自报计数自相矛盾**、**2 处引用悬空**（含一个已进入每会话注入的死引用） | **实测·自有** |
| **框架漏洞审查** | 21 条，**零驳回**（与给全材料直接相关）；且**重述了 `02` §7.6 四条挂多轮未修的自相矛盾** | **实测·自有** |
| **T3 对照** | 第三轨道首跑：Kimi 出一致性类 7 条 / 符合性类 1 条，GPT-6 出 2 / 6 —— **两家仅 2 条重叠** | **实测·自有** |
| **已知偏差** | **按文件名检索会漏掉内容命中的文件** —— 由此产生 F-07 假阳性（报「工作区无标定模板」，而 `任务书模板.md` 一直存在）。**⇒ T1–T4 类检索式断言须交叉复验。** | **实测·自有** |
| **能力排名** | **空白** | **未测** |

---

## 三、GPT-6 Astra（T3 规格符合性）

| 证据 | 内容 | 级别 |
|---|---|---|
| **T3 重跑** | 12 条发现 → **8 采纳 / 2 驳回 / 2 待定**；其中 **B-01（energy-based OOD 判据方向反了）是真错**，主控自查发现不了 | **实测·自有** |
| **★ 已知稳定偏差（已进安全边界⑧）** | **在「某条要求是否落实」上存在稳定假阳性** —— **两次**都误报 `02_任务二….md` 缺温度标定，而该文件 L336 明写有。**⇒ 其 A 类（漏答）发现必须逐条反证核对。** | **实测·自有** |
| **能力排名** | 榜单称 #1/196（88.47）/ Terminal-Bench 66.4% —— **本表不采用** | ~~榜单~~ |

---

## 四、明确的空白（**不拿榜单填**）

| 空白 | 影响 | 唯一解法 |
|---|---|---|
| **没有任何模型做过真实重型编码 A/B** | 「复杂 coding → Fable 5.1」是**靠榜单定的**，而安全边界③禁止用榜单选型 ⇒ **F-11 结构性自相矛盾** | 一件真实的重型编码任务 |
| **主控的能力档位未与任何人正面比较过**（除 L-001 一次） | 上面 §一 的选择性质疑 | 同上 |
| **Fable 5.1 / Opus 5.5 从未在本工作区跑过真实任务** | 只实测过「能调通 + 计费吻合」，**质量完全未验** | 同上 |
| **GLM-5.3 的真实多轮缓存命中率** | 现值 ¥（略）/轮是**下界**（用逐字节相同请求测得） | 一次真实多轮任务 |

---

## 五、诚实边界

1. **本档案由主控自己编写，而它评的正是主控自己。** 按准则①，**本文件本身需要独立复核**（走 T6）—— **本轮因预算已达 ¥（略） / 熔断线 ¥60，未派发。**
2. **`N=1` 的证据被明确标注为「实测·单次」** —— 它不能支撑任何排序结论（对照：V-001~004 因天花板效应测不出差异，恰恰说明单次实验的分辨率不足）。
3. **所有「空白」都不是"没有证据"，而是"没测过"** —— 这两者的区别是本文件存在的意义。
4. **本文件会随证据增加而过期**，但不设有效期 —— 因为**每次新增实测都是它的更新触发器**（见 `05_快照与再标定.md` §三）。

---

## 六、★ 回顾性标定：核验者与裁定的**历史成绩**（第 9 轮新增）

> **为什么补这一节**：第 9 轮拿本体系去对照**外部文献**（LLM-as-a-judge 的已知失效模式）时，发现一条最深的要求 ——
> **判官必须与"已知真值 / 人类标注"标定，否则它的准确率是未知的**（见 [Item-Level Blinded Scoring and Inter-Rater Reliability](（外部文献链接已略 —— 原链接的路径含可定位到具体作者的标识） Validation](https://ieeexplore.ieee.org/document/11677987)）。
> **而本体系的核验者，从来没有任何"准确率"数字** —— 我们只知道它有偏差（GPT-6 的假阳性、Kimi 的检索盲区），**不知道它有多准**。
> **⇒ 这一节用**已发生的历史**做一次标定。零成本（全部数据已存在）。**

### 6.1 核验方的「采纳率」（历史样本，非实验）

| 轨道 | 核验方 | 发现数 | 主控裁定：采纳/成立 | 部分成立 | 驳回 | **采纳率** |
|---|---|---|---|---|---|---|
| 框架漏洞审查 | Kimi K3 | 21 | 20 | 0 | 0（1 条需用户） | **≈ 100%** |
| **第三轨道 T3** | **GPT-6 Astra** | **12** | 8 | 1 | **2** | **75%（9/12）** |
| T5 代码核验 | 主控自写（模式 A） | 3 | 3 | 0 | 0 | 100% |
| T7 对抗性推演 | Kimi K3 | 25 | 18 | 3 | **0** | **84%（21/25，另 3 条需用户）** |

**读法（重要）**：
- **这不是"准确率"** —— **真值本身是主控的裁定，而主控的裁定未经外部核验**。它只能说明「核验方与主控的一致程度」。
- **采纳率高≠核验方准**：也可能意味着**主控容易接受与自己一致的结论**（这正是准则①要防的）。
- **⇒ 它是"一致率"，不是"正确率"。** 名字必须写对。

### 6.2 ★★ 唯一一个"外部对主控的评分"数字

**T6（Kimi K3，换家族）复核了主控对 21 条框架漏洞的裁定本身**：

| T6 的判定 | 条数 | 占比 |
|---|---|---|
| **不相符**（R1：引用不存在等） | **1** | **4.8%** |
| **半相符**（部分证据不支撑结论） | **5** | **23.8%** |
| 相符 / 基本相符 / 结论相符 | 15 | 71.4% |
| **⇒ 有问题的合计** | **6** | **28.6%** |

**⇒ 这是本体系里唯一一个「**别人给主控打的分**」：主控做的裁定里，约 **29%** 被另一个模型家族判为「与证据不完全相符」。**
**它的分量在于**：这 29% **主控自己是看不出来的**（如果看得出来，当初就不会那么裁）。**⇒ 这是"自己批自己"这一自指环的量化代价。**

### 6.3 这两组数字怎么用（**不许过度解读**）

| 允许 | 不允许 |
|---|---|
| 说「主控裁定的自洽性有约 29% 的已知缺陷率（据 T6 一轮）」 | ❌ 说「主控准确率 71%」—— T6 本身未经核验 |
| 说「GPT-6 的采纳率低于 Kimi 与 T7，且它有已实证的稳定假阳性」 | ❌ 说「Kimi 比 GPT-6 准」—— 采纳率高可能只是"更迎合主控" |
| 用它**决定"下一步该测什么"** | ❌ 用它**替代**真实标定集（带已知真值的材料） |

### 6.4 下一步该建的东西（**记在这里，别忘**）

**「核验者标定集」**：构造一批**带已知真值**的材料（植入 N 个**已知缺陷** + M 个**已知正确项**），让核验方跑，量出**灵敏度与特异度**。
> **这正是我们对"代码"做过的事** —— T5 的区分度标定就是"注入 bug 看测试是否失败"。
> **但从没对"核验者本身"做过。** 这是本档案目前最大的一个空白。
