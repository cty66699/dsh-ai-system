// 快照复查 —— **本子集的占位实现**
//
// 真正的实现（`_refresh_snapshots.cjs`）会去查一批**会过期的事实**：模型价格、通道倍率、版本读数。
// 它**依赖私有通道参数**，因此不随本子集发布。
//
// 那为什么还要放这个文件、而不是什么都不放？
//   因为**「根本没跑」和「跑了发现有东西」是两件事**，输出里必须长得不一样：
//     · 不存在的脚本 ⇒ 调用方拿到"模块找不到"，报成 **⚠️ 有发现（退出码 1）** —— **这是假红**；
//     · 这个占位实现 ⇒ 明确打印原因并以 **3（跳过）** 退出 ⇒ 输出里显示 **⏭️ 未配置**。
//   **假红和假绿同族：判据坏了，结论就不可信 —— 不管它指向哪个方向。**
//
// 用法: node _refresh_snapshots.cjs
// 退出码: 3 = 跳过（未配置）
console.log('⏭️  快照复查：**本子集未包含实现** —— 真实现依赖私有通道参数（价格/倍率/版本读数）。')
console.log('   本项跳过。要自己用得填进你自己的复查对象 —— 见 docs/治理规则.md 里「会过期的事实」那一节。')
process.exit(3)
