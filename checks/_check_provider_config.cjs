// 配置 section 存活 —— **本子集的占位实现**
//
// 真正的实现会跑 `dsh --profile <p> --dump-config`，检查 settings 里的 section
// 有没有在升级 / 迁移时被运行时**静默丢弃**。
//
// 为什么需要它（2026-09-25 作者的真实事故）：
//   升级 DSH 后，legacy import 声称「把 settings.yaml 的 section 迁移到 active profile」——
//   **简单 section 迁移成功，唯独装全部模型路由的那个 section 被静默拒绝** ⇒
//   运行时拿到的 providers 是空的 ⇒ 除一个走专用包的 provider 外**全部报 NO_ADAPTER**，
//   而**日常对话毫无异常**（作者自己走的那条通道恰好幸存），只有"换模型家族"时才暴露。
//   ⇒ **"配置在"不等于"配置被运行时读到"** —— 这就是本项要盯的那条缝。
//
// 它**依赖作者本机的 DSH 安装与 profile**，因此不随本子集发布。
//
// 那为什么还要放这个文件、而不是什么都不放？
//   因为**「根本没跑」和「跑了发现有东西」是两件事**，输出里必须长得不一样：
//     · 不存在的脚本 ⇒ 调用方拿到"模块找不到"，报成 **⚠️ 有发现（退出码 1）** —— **这是假红**；
//     · 这个占位实现 ⇒ 明确打印原因并以 **3（跳过）** 退出 ⇒ 输出里显示 **⏭️ 未配置**。
//   **假红和假绿同族：判据坏了，结论就不可信 —— 不管它指向哪个方向。**
//
// ★ 这个判据是可以移植的：**任何"升级 / 迁移后配置静默失效"的场景都适用**。
//   做法：跑一次你的配置解析器（或 dump 出"运行时实际看到的配置"），
//   确认目标 section 在**解析后的结果**里可见 —— 而不是在源文件里可见。
//
// 用法: node _check_provider_config.cjs
// 退出码: 3 = 跳过（未配置）
console.log('⏭️  配置存活：**本子集未包含实现** —— 真实实现要跑目标程序的配置 dump 并读作者本机的 profile。')
console.log('   本项跳过。要自己用得先确定"你要盯哪个 section"，再确认它在**解析后的配置**里可见（不是源文件里）。')
process.exit(3)
