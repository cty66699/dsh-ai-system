// 验证代表性 —— **本子集的占位实现**
//
// 真正的实现检查：**验证用的 profile 是否覆盖了生产 profile 的组合**。
//
// 为什么需要它（2026-09-25 作者的真实事故）：
//   作者用 `--profile headless` 做了一次"真实模型调用"验证，拿到了通过，
//   据此认为生产 profile 可用 —— 而那个精简 profile 只有 2 个 bundle、
//   **比生产少 10 个**（含全部第三方插件），当时生产上 8 条模型通道已经全挂。
//   ⇒ **这就是假阳性**：那次"通过"根本没碰到出问题的那部分代码。
//   ⇒ 一句话判据：**"用小环境验证"不等于"大环境可用"**。
//
// 它**依赖作者本机的 profile 目录**，因此不随本子集发布。
//
// 那为什么还要放这个文件、而不是什么都不放？
//   因为**「根本没跑」和「跑了发现有东西」是两件事**，输出里必须长得不一样：
//     · 不存在的脚本 ⇒ 调用方拿到"模块找不到"，报成 **⚠️ 有发现（退出码 1）** —— **这是假红**；
//     · 这个占位实现 ⇒ 明确打印原因并以 **3（跳过）** 退出 ⇒ 输出里显示 **⏭️ 未配置**。
//
// ★ 这个判据是可以移植的，而且**很便宜**：
//   凡是用"精简组合"做的验证，先列一遍"**它比生产少了什么**"——
//   少的东西里如果有你这次要验证的对象，那个"通过"就不能外推。
//
// 用法: node _check_verify_profile.cjs <验证用的组合> [基线组合]
// 退出码: 3 = 跳过（未配置）
console.log('⏭️  验证代表性：**本子集未包含实现** —— 真实实现要读作者本机的 profile 目录。')
console.log('   本项跳过。要自己用得先定义"你的生产组合"是什么，再检查"验证用的组合是否覆盖了它"。')
process.exit(3)
