# 做这套东西时，我学到的东西

> 这不是一份"我怎么做的"报告 —— 那部分在 `README.md` 里。
> **这一份讲的是：它为什么长成现在这样，以及我在这个过程里改掉了哪些想法。**
> 每一条都附**它产生的那个现场** —— 没有现场支撑的规则，过一阵就没人知道它为什么存在了。

---

## 一、起点：一次具体的失信

这套东西不是为了"做一个工具"而做的。它起源于一件很小、很难堪的事：

**我写了十几条检查，而它们从来没有拦下过任何东西。**

不是坏了 —— 是**它们躺在那里等人想起来跑**。我改了代码，忘了跑检查；改了源，忘了重新生成；
写了"落实了"三个字，检查就认为落实了。**每一次都是别人（使用者、访客、另一个模型）先发现的。**

> **⇒ 于是这套东西的第一条设计原则是**：
> **「工具存在」与「工具在流程里」是两件事 —— 只有后者算数。**
> 所以有了**统一入口**：一条命令跑完所有断言，而"某个检查有没有进这个入口"由另一条断言守着。

---

## 二、主线：机制是对的，但没人守它

做到后来我发现，这几十轮里反复出现的其实是**同一个形状**：

| 现场 | 它是怎么空转的 |
|---|---|
| 一个账本的 `round` 字段**停在第 12 轮** | 而实际已经跑到第 29 轮 —— **17 轮的欠账没人提醒** |
| 一个模板**不被任何检查覆盖** | 而它会被注入使用者的**每一个会话** |
| 一个断言**从没被注入过缺陷** | 于是没人知道它到底会不会咬人 |
| 一份对账清单**写死了 3 个文件** | 而实际有 12 个源 —— **另外 9 个改了没人知道** |

**⇒ 它们的共同点不是"没人写"，而是「**写了，但没有东西守着它继续成立**」。**

> **⇒ 所以这几十轮做的，基本上就是把「没人守」逐个变成「有东西守」。**

---

## 三、七条我改掉的想法

### ① 「假绿」和「假红」是同一个病

我一开始只防"检查没发现问题"（假绿）。后来撞上另一半：**检查把正确的判成了错误的**。

> **⇒ 判据坏了，结论就不可信 —— 不管它指向哪个方向。**
> 而**误报比漏报更糟**：漏报是"你没发现"，误报是"**让使用的人不再相信这个检查**"。

**现场**：一条新加的引用检查一次报了 **229 处**，全是误报（扫到了第三方包解包物）——
**而它的修法不是"把判据改松"，是"跳过 + 写明理由"**。

### ② 「列举」会以七种方式悄悄漂移

最开始的清单是手写的。后来我数出来，**"列举会漂移"至少有七种形态**：

写死项数 · 写死文件名 · 一处写了两遍 · 语义相同而写法不同 · 从名字猜（而不是按形状解析）·
**同一个真源被四处各印一遍** · 以及最坏的那种 —— **它根本不在任何清单上**。

> **⇒ 唯一稳定的做法：从**唯一真源**派生。** 而派生不出来的地方，就**让它在输出里自己说出来**。

### ③ 「范围」有四张面孔，而我每次都栽在同一张上

一个会遍历文件的检查，它的行为由三件事**隐式**决定：**目录结构 + 跳过规则 + 配置回落** ——
而这三件事**各自都变过**。四个实例跨了四十多轮：

| 面孔 | 撞到的事 |
|---|---|
| **扫描范围**（读到哪些）| 一条新检查报了 229 处误报 |
| **切分范围**（切掉哪些）| 一个"从这里到文末全切"的正则 —— **没有任何地方写着"从这里往后不公开"** |
| **发布范围**（出去哪些）| 脚本有发布出口，**而手写的源没有** |
| **写入范围**（会改哪些）| 我把「这个工具只读你的项目」当成一句话说了很久 —— **直到把它变成一条能算哈希的断言** |

> **⇒ 判据你会反复推敲，而「范围」你通常一次定完就不再想它 —— 而四次出问题，四次都是范围。**

### ④ 我建的每一个判据，都会先抓到我自己

这一条是最近才成形的，而它三次应验：

- 我新建一个"跑完检查后工程有没有被碰过"的断言 ⇒ **它第一次运行，抓到我三个 bug**（其中一个是我二十多轮前踩过的同一个坑）；
- 我新建一条"孤儿脚本"检查 ⇒ **它第一次跑就抓到三个该处置的东西**；
- 我**跑一个已经用了很久**的检查 ⇒ **它自己崩了**，露出两个真缺陷。

> **⇒ 触发它的不只是"新判据的第一次运行"，而是「一个判据第一次真正用到它自己身上」。**
> 所以我现在的心态是：**期待它抓到我，而不是期待它全绿。**
> **反过来：如果一条新检查第一次跑就全绿，那是可疑的。**

### ⑤ 读数有三种坏法，而它们长得一模一样

我在这上面栽过四次，才把它从"口诀"变成"动作"：

| 坏法 | 看起来像 | 真相 |
|---|---|---|
| **注入没生效** | "那个检查不管用" | 我的测试根本没改到东西 |
| **尺子坏了** | "那个对象坏了" | 我读结论用的那条命令被干扰了 |
| **结论句骗人** | "它说没事" | 输出那句总结**没跟着判据一起改** |

> **⇒ 所以我建了一个"量具校准"脚本**：拿一对已知输入（一个该失败、一个该成功）跑同一条命令，
> 看它给不给预期结果。**在尺子不准之前，不要相信用它读出来的任何结论。**

### ⑥ 「存在某个保障」与「那个保障能用」是两件事

**这条是我最后才撞上的，而它最难自查。**

文档里写着「动手前会存一份带时间戳的备份 —— **改坏了可以用它退回去**」。
读起来很负责。**而"怎么退"一个字没写。**

> **⇒ 它的危险在于「读起来是完整的」** —— 错别字、逻辑矛盾、悬空引用都有形状可循，
> **而"写了半句好话"没有形状**：它语法正确、语气负责，
> **唯一的问题是它没告诉读者下一步做什么。**
>
> **⇒ 而作者的自查对它无效**（作者知道自己会怎么退，所以看不出缺什么）——
> **只能靠"把文档交给一个真会照做的人"。**

### ⑦ 记忆、断言、外部复核，是三件不同的事

我一度以为"记下来"就等于"不会再犯"。数据否掉了这个想法：

> 同一条内容 —— **写成教训** ⇒ 记完之后照样踩；**写进每次会话都注入的指令** ⇒ 同类错照样犯；
> **写成断言放进统一入口** ⇒ 下一次**是脚本自己抓到的**。

> **⇒ 记忆管「我想什么」，断言管「我做什么」，换家族复核管「我做得对不对」。**
> 三者混用，就会出现"记忆里写着、实操照样错"。

---

## 四、还没做到的（放在这里，不藏在角落）

| 没做到 | 为什么 |
|---|---|
| **在真正的干净机器上装一遍** | 需要**另一台机器** —— 我用的是"干净目录 + 影子 HOME"，**同一台机器、同一个 Node** |
| **Linux / macOS 实测** | 只把 `process.platform` **伪装成 linux** 跑过断言层 |
| **PowerShell 7 的行为** | 本机只有 5.1（**5.1 上每次都真跑过**）|
| **外围核验只跑了一家** | 关键交付物本该"换家族双跑" —— 单跑会漏，而这一次是单跑 |

**⇒ 这三条不是"以后再补"，而是"在当前条件下补不了"** —— 所以它们的正确写法就是「未验证」。

---

## 五、一句话

> **这套东西的价值不在它有多少条断言，而在它把多少句「我以为」变成了「算出来的」。**
>
> 而它最诚实的地方，是它**把自己的边界也写进了产物里** ——
> 包括哪一道闸从没被触发过、哪一条判据防的事还没发生、以及**哪三件事我根本没验过。**